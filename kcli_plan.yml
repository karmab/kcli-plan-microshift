microshift_profile:
 type: profile
 domain: {{ domain }}
 image: {{ image }}
 memory: {{ memory }}
 numcpus: {{ numcpus }}
 files:
 - 01_clients.sh
 - 02_crio.sh
 - 03_microshift.sh
 scripts:
 - deploy.sh

{% for number in range(0, nodes) %}
{{ cluster }}-{{ number }}:
  profile: microshift_profile
{% endfor %}
